pipeline
{
    agent any
    stages
    {
        stage('CHECKOUT')
        {
            steps
            {
                git 'https://github.com/Sushil-Ohol/ENVIRONMENT-VARS.git'
                echo 'CHECKING OUT REPOSITORY' 
            }
           
        }
    }
}
